// This file tells Next.js to serve the favicon from the public directory
export const runtime = 'edge';

export default function Favicon() {
  // This is just a placeholder, the actual favicon.ico will be served from public directory
  return null;
}
